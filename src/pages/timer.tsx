//import { useContext } from "react";
//import { TimerContext } from "@/contexts/timer-context";
//import { DataContext } from "@/contexts/data-context";

export function Timer() {
    //const { formattedTime, startTimer, stopTimer, pauseTimer, running } = useContext(TimerContext);
    //const { categories } = useContext(DataContext);

    return (
        <div className="grid grid-cols-6 pr-[80px] gap-4">
            <div className="col-span-4">zsd</div>
            <div className="col-span-2">zsd</div>
        </div>
    );
}
